//! Thin binary over the `lb` library. Config loading and the listeners
//! arrive with the next steps; until then this only proves the wiring.

fn main() {
    println!("lb: nothing to serve yet (scaffold)");
}
